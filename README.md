# Eigen OS MkDocs (quantum theme)

## Local dev
```bash
pip install -r requirements.txt
mkdocs serve
```

## Deploy
GitHub repo Settings → Pages → Source: **GitHub Actions**.

Custom domain:
- Set domain in Settings → Pages → Custom domain
- Edit `docs/CNAME` if you want the CNAME committed in the site output
