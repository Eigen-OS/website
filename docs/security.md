# Security

Report vulnerabilities privately.

- Email: `security@eigen-os.org`

!!! warning "Do not open public issues for vulnerabilities"
    Use a private channel first.

Add a `security.txt` file at `/.well-known/security.txt` when you publish the website.
