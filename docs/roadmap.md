# Roadmap

## MVP

- Freeze contracts (proto + RFC)
- End-to-end jobs: submit → compile → execute(sim) → results
- Basic CLI and system API

## Next

- More backends (HAL drivers)
- Observability (logs, traces, artifacts)
- Registry/marketplace experiments
